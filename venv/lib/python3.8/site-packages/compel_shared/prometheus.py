import shutil
from pathlib import Path

from compel_shared.config import PROMETHEUS_MULTIPROC_PATH


def init_prometheus():
    path = PROMETHEUS_MULTIPROC_PATH
    if path:
        path = Path(path)
        if path.exists() and path.is_dir():
            shutil.rmtree(path)
        path.mkdir(parents=True)
